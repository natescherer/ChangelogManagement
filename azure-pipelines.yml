# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

strategy:
  matrix:
    linux:
      imageName: ubuntu-16.04
    mac:
      imageName: macos-10.13
    windows:
      imageName: vs2017-win2016

variables:
  GitName: Nate Scherer
  GitEmail: 376408+natescherer@users.noreply.github.com
  PowerShellModules: ChangelogManagement,InvokeBuild,Pester,platyPS,MarkdownToHtml,BuildHelpers,PowerShellGet

pool:
  vmImage: $(imageName)

steps:
- powershell:
    targetType: inline
    script: |
      Install-Module -ModuleName Pester  -Force -SkipPublisherCheck
      Invoke-Pester
